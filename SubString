두 가지의 종류가 있다.

1. 인자값을 하나만 받는 함수.
2. 인자값을 2개를 받는 함수.


1.
string str = "0123456789";
str.substring(5)

를 한다면 -> 56789 의 값을 리턴한다.

2.
string str = "0000003565120";
str.substring(6,13);
str.substring(6);
--> 동일한 3565120  의 값을 리턴한다.


만약, 범위를 벗어나는 구역을 한다면 범위를 벗어났다는 에러문이 나온다.
